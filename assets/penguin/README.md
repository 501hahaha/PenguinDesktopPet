# Penguin assets

The supplied design-board image is stored as `penguin_design_sheet.png` for reference.

The raw frame sheet named `penguin_sprite_sheet.png` was not found in the workspace yet. The frame splitter should run only after that raw sheet is provided, because the design board contains labels, folders, and multiple unrelated asset groups.

Redistribution rights and attribution for the supplied artwork and animation media must be confirmed before the first public release. Do not publish these assets until the project owner has confirmed that they may be redistributed with the application.
